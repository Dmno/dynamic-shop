{% extends 'admin/base.html.twig' %}

{% block body %}
    <div class="main">
        <div class="adminHeader">
            {{ action }}
        </div>

        <div class="content">
            {{form_start(form)}}
            {{form_errors(form)}}
            {{form_widget(form)}}
            {{form_end(form)}}
        </div>
    </div>
{% endblock %}